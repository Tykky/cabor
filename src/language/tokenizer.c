#include "tokenizer.h"


cabor_vector tokenize(const char* buffer, size_t size)
{
    cabor_vector vector = create_cabor_vector(TOKENIZER_VECTOR_DEFAULT_CAPACITY, CABOR_CHAR, true);

    return vector;
}

